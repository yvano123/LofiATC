LofiATC application

## Steps to run (Source)
- Clone the repo
- cd into the repo using a terminal
- run `npm i`
- run `npm run dev`
- open the shown url

## Steps to run (Build)
- unzip the release zip
- cd into the unzipped folder using a terminal
- run `npx serve`
- open the shown url
